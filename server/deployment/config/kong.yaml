_format_version: "3.0"

services:
  - name: user-management-service
    url: http://user-management-service:3000
    routes:
      - name: user-management-route
        paths:
          - /user-management

  - name: service-management-service
    url: http://service-management-service:3000
    routes:
      - name: service-management-service
        paths:
          - /service-management

  - name: action-service
    url: http://action-service:3000
    routes:
      - name: action-service
        paths:
          - /action

  - name: reaction-service
    url: http://reaction-service:3000
    routes:
      - name: reaction-service
        paths:
          - /reaction

  - name: area-composition-service
    url: http://area-composition-service:3000
    routes:
      - name: area-composition-service
        paths:
          - /area-composition
